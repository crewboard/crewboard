class: ChatMessage
table: chat_message
fields:
  roomId: int
  parentMessageId: int?
  userId: int # Sender ID
  message: String
  messageType: MessageType
  seenUserList: List<int> # List of user IDs who saw the message
  sameUser: bool # UI optimization from old project
  deleted: bool
  createdAt: DateTime
